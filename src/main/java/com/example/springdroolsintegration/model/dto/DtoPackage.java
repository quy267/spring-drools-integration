package com.example.springdroolsintegration.model.dto;

/**
 * This package contains Data Transfer Objects (DTOs) for the Spring Drools Integration application.
 * DTOs are used to transfer data between the service layer and the controller layer,
 * and for API responses to clients.
 */
public class DtoPackage {
    // This is a marker class to define the package
}